# Skill Registry — viandas-stacklab

> Auto-generated by sdd-init. Last updated: 2026-05-17.

## Project Convention Files

| File | Path | Type |
|------|------|------|
| AGENTS.md | `~/.config/opencode/AGENTS.md` | Global persona/rules (Gentle AI protocol) |

Convention summary (from AGENTS.md):
- Conventional commits only, no "Co-Authored-By"
- Short answers by default, expand only when needed
- Ask one question at a time, stop and wait
- Verify technical claims before stating them
- Persona: Senior Architect, 15+ years, GDE & MVP
- Spanish replies use Rioplatense Spanish (voseo)
- Engram persistent memory protocol mandatory

## Skills

### branch-pr

- **Trigger**: creating, opening, or preparing PRs for review
- **Path**: `~/.config/opencode/skills/branch-pr/SKILL.md`
- **Compact Rules**:
  - Every PR MUST link an approved issue. Exactly one `type:*` label.
  - Branch names: `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`
  - PR body requires: linked issue (Closes/Fixes/Resolves #N), type checkbox, summary, changes table.
  - Automated checks must pass before merge.

### chained-pr

- **Trigger**: PRs over 400 lines, stacked PRs, review slices
- **Path**: `~/.claude/skills/chained-pr/SKILL.md`
- **Compact Rules**:
  - Split PRs over 400 changed lines unless `size:exception`.
  - Each PR reviewable in ≤60 min. One deliverable work unit per PR.
  - Feature Branch Chain: tracker PR (draft/no-merge), child targets parent branch.
  - Include dependency diagram marking current PR with 📍.
  - Polluted diffs are base bugs: retarget or rebase. Do not mix chain strategies.

### cognitive-doc-design

- **Trigger**: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs
- **Path**: `~/.claude/skills/cognitive-doc-design/SKILL.md`
- **Compact Rules**:
  - Lead with answer. Progressive disclosure (happy path → details → edge cases).
  - Chunk into small sections. Prefer tables, checklists, examples over prose.
  - PR docs: state review order, what's out of scope, prev/next PR links.
  - Use `gh pr view` to check changed-line count for cognitive load budget.

### comment-writer

- **Trigger**: PR feedback, issue replies, reviews, Slack messages, or GitHub comments
- **Path**: `~/.config/opencode/skills/comment-writer/SKILL.md`
- **Compact Rules**:
  - Start with actionable point. Be warm and direct. 1-3 short paragraphs max.
  - Explain why technically when requesting changes.
  - Match thread language. Spanish: Rioplatense/voseo (podés, tenés, fijate, dale).
  - No em dashes. Don't pile on every tiny preference; comment on highest-value issue.

### customize-opencode

- **Trigger**: editing opencode config (opencode.json, .opencode/, ~/.config/opencode/)
- **Path**: built-in (no file)
- **Compact Rules**:
  - ONLY for opencode's own configuration: agents, subagents, skills, plugins, MCP servers, permission rules.
  - NEVER use for user's application code or projects not configuring opencode itself.

### go-testing

- **Trigger**: Go tests, go test coverage, Bubbletea teatest, golden files
- **Path**: `~/.claude/skills/go-testing/SKILL.md`
- **Compact Rules**:
  - Prefer table-driven tests with `t.Run(tt.name, ...)`. Test behavior, not implementation.
  - Use `t.TempDir()` for filesystem tests, `testing.Short()` for integration skip.
  - Bubbletea: `Model.Update()` for state, `teatest` only for interactive flows.
  - Golden files must be deterministic. Update via `-update` flag, re-run without.

### issue-creation

- **Trigger**: creating GitHub issues, bug reports, or feature requests
- **Path**: `~/.claude/skills/issue-creation/SKILL.md`
- **Compact Rules**:
  - Blank issues disabled. Must use template (Bug Report or Feature Request).
  - All issues auto-labeled `status:needs-review`. Maintainer must add `status:approved` before PR.
  - Questions go to Discussions, not issues. Search duplicates first, fill ALL required fields.

### judgment-day

- **Trigger**: judgment day, dual review, adversarial review, juzgar
- **Path**: `~/.config/opencode/skills/judgment-day/SKILL.md`
- **Compact Rules**:
  - Launch two blind judges in parallel. Never review code yourself. Wait for both.
  - WARNING (real) only if normal intended use triggers it; else downgrade to INFO.
  - Ask before fixing Round 1 confirmed issues. Re-launch judges after fix.
  - Terminal states: APPROVED ✅ or ESCALATED ⚠️. Max 2 fix iterations with remaining issues.

### skill-creator

- **Trigger**: new skills, agent instructions, documenting AI usage patterns
- **Path**: `~/.claude/skills/skill-creator/SKILL.md`
- **Compact Rules**:
  - Skill is LLM runtime contract, not human docs.
  - `description`: one physical line, quoted, trigger-first, ≤250 chars.
  - Frontmatter: name, description, license, metadata.author, metadata.version.
  - Structure: Activation Contract → Hard Rules → Decision Gates → Execution Steps → Output Contract → References.
  - Target 180-450 body tokens, hard max 1000. Supporting material in `assets/` or `references/`.

### work-unit-commits

- **Trigger**: implementation, commit splitting, chained PRs, keeping tests and docs with code
- **Path**: `~/.claude/skills/work-unit-commits/SKILL.md`
- **Compact Rules**:
  - Commit by work unit (deliverable behavior), not by file type.
  - Keep tests with the code they verify. Keep docs with user-visible change.
  - Each commit: one clear purpose, repo still makes sense, reasonable rollback.
  - SDD workload guard: group commits into chained PR slices if >400 lines forecasted.
